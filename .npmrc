# Zora Coins SDK 0.7.x pins viem 2.53.1 exactly even though the project uses
# a compatible security-fixed viem release. Keep audits enabled and allow npm
# to resolve that upstream peer-range lag until the SDK widens its peer range.
legacy-peer-deps=true
audit=true
